// Assuming 'context' is the canvas drawing context
var x1 = 100; // example x-coordinate
var y1 = 100; // example y-coordinate
var rx = 50;  // example x-offset
var ry = 50;  // example y-offset

context.lineTo(x1 + rx, y1 + ry);
